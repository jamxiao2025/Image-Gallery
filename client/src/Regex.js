export const Shoes = new RegExp(
  '(shoe)', 'i'
  /*Shoes, shoe, shoe-anything should work, i designates case insensitive*/
)
export const Car = new RegExp(
  '(car)', 'i'
  /*Shoes, shoe, shoe-anything should work, i designates case insensitive*/
)
